<?php
/**
 * Loading function for all product resources in a directory
 * @param String $ordner path to load images
 */
function load($product)
{
	$pdir = scandir($product->respath); // Sortierung A-Z
	foreach ($alledateien as $datei) {
	if(is_dir($filename))
	{
		
		
	}
// 		$dateiinfo = pathinfo($ordner."/".$datei);

// 		if ($datei != "." && $datei != ".."  && $datei != "_notes") {
// 		};
// 	};
	}
}
?>